# Normalize to LF in the repository. Vercel builds on Linux; keeping LF in
# history stops Windows checkouts from producing whole-file phantom diffs.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
